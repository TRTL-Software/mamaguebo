{
  "name": "image-site-workers",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "dev:malditomarico": "wrangler dev --env malditomarico",
    "deploy": "wrangler deploy",
    "deploy:malditomarico": "wrangler deploy --env malditomarico",
    "deploy:all": "wrangler deploy && wrangler deploy --env malditomarico",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250320.0",
    "typescript": "^5.8.2",
    "wrangler": "^4.0.0"
  }
}
